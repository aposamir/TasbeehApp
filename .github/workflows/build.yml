name: Build Android APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Prepare signing keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            echo "$KEYSTORE_BASE64" | base64 -d > app/release.keystore
            echo "Using the fixed signing key from the KEYSTORE_BASE64 secret"
          else
            echo "::warning::KEYSTORE_BASE64 secret is not set - signing with a new random key, so this APK cannot be installed over a previous version"
            keytool -genkeypair -v \
              -keystore app/release.keystore \
              -alias tasbeehapp \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -storepass android -keypass android \
              -dname "CN=TasbeehApp, OU=Dev, O=aposamir, L=City, ST=State, C=SA"
          fi

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.4'

      - name: Build release APK
        run: gradle assembleRelease

      - name: Zip APK
        run: |
          cd app/build/outputs/apk/release/
          zip TasbeehApp-release.zip *.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: TasbeehApp
          path: app/build/outputs/apk/release/

      - name: Remove previous release so only the newest build remains
        if: github.event_name != 'pull_request'
        run: gh release delete latest --yes --cleanup-tag || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish GitHub Release with APK
        if: github.event_name != 'pull_request'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "أحدث نسخة"
          files: app/build/outputs/apk/release/TasbeehApp-release.zip
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
